import React from "react";
import { AuthLayout, RegisterForm } from "../components";

export const AuthRegisterPage = () => {
  return (
    <AuthLayout>
      <RegisterForm />
    </AuthLayout>
  );
};
